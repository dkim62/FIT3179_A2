{
    "$schema": "https://vega.github.io/schema/vega-lite/v5.json",
    "description": "A population pyramid for the US in 2000, created using stack. See https://vega.github.io/vega-lite/examples/concat_population_pyramid.html for a variant of this created using concat.",
    "data": { "url": "https://raw.githubusercontent.com/dkim62/FIT3179_A2/main/Data/Crash_Data.csv"},
    "transform": [
      {"calculate": "datum.Gender == 'Female' ? 'Female' : 'Male'", "as": "gender"},
      {"calculate": "datum.Gender == 'Female' ? -datum.Case : datum.Case", "as": "signed_people"}
    ],
    "width": 400,
    "height": 400,
    "mark": "bar",
    "encoding": {
      "y": {
        "field": "Age_Group",
        "title" : "Age"
      },
      "x": {
        "aggregate" : "sum",
        "field": "signed_people",
        "axis": {"format": "s"},
        "title" : "number of incidence"
      },
      "color": {
        "field": "Gender",
        "scale": {"range": ["#675193", "#ca8861"]},
        "legend": {"orient": "top", "title": null}
      }
    },
    "config": {
      "view": {"stroke": null},
      "axis": {"grid": false}
    }
  }